#include "common.ch"
#include "inkey.ch"

static Static1:= ""

********************************
function READVAR(Arg1)

   local Local1
   Local1:= Static1
   if (ISCHARACTER(Arg1))
      Static1:= Arg1
   endif
   return Local1

* EOF
